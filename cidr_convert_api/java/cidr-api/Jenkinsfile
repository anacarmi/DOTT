pipeline {
	agent any
		stages {

			stage('Encontrar Dockerfile, ejecutarlo') {
				steps {
					dir("cidr_convert_api/java/") {
						sh "sudo docker images" 
					}
					
				}
			} 

			stage('Three') {
				steps {sh 'pwd'}
			}
		}
} 

 
