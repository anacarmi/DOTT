pipeline {
	agent any
		stages {

			stage('') {
				steps {
					dir("cidr_convert_api/java/") {
						sh "./build.sh" 
					}
					
				}
			} 

			stage('Three') {
				steps {sh 'pwd'}
			}
		}
} 

 
