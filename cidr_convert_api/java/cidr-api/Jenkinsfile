pipeline {
	agent any
		stages {
			stage('') {
				steps {
					dir("cidr_convert_api/java/") {
						sh 'pwd'
						sh 'ls'
						sh 'chmod +x build.sh'
						sh 'echo "hello"'
						// sh "./build.sh" 
					}
					
				}
			} 

			stage('SonarCloud') {
				steps {sh 'pwd'} 
			}
		}
} 

 
